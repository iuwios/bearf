
{%- style -%}
@media screen and (max-width: 767px) {    
.mobile-image-divider{
  margin-bottom:40px;
}
}
{%- endstyle -%}

{% comment %} Settings {% endcomment %}
{%- liquid
  assign show_nav_arrows = section.settings.show_nav_arrows
  assign dots_style = settings.dots_style
  assign imageStyle = section.settings.image_style
  assign homepageStyle = section.settings.homepage_style
  if dots_style == 'hidden' or section.blocks.size < 2
    assign dots = false
  else
    assign dots = true
  endif
  assign transition = section.settings.transition
  assign autoplay = section.settings.autoplay
  assign duration = section.settings.duration | times:1000
  unless autoplay
    assign duration = false
  endunless
  assign fade_mode = true
  if transition == 'slide'
    assign fade_mode = false
  endif
  assign hover = true
  assign transition_text = settings.transition_text
  assign animation_delay = 0
  assign animation_anchor = '#gallery-module--' | append: section.id

  assign image_position = section.settings.slide_image_position | default: 'left'
  assign image_width = section.settings.slide_image_width | split: '|' | first | default: 'med_s35'
  assign content_width = section.settings.slide_image_width | split: '|' | last | default: 'med_s25'
-%}

{% comment %} Check if Image Height setting is selected {% endcomment %}
{%- liquid
  assign image_height = false
  if section.settings.slide_height == 'original-height'
    assign image_height = true
  endif
-%}

{% comment %} Content {% endcomment %}
{%- liquid
  assign title = section.settings.title
  assign title_color = section.settings.title_color
  assign text = section.settings.content | truncatewords: 70
  assign button_text = section.settings.cta
  assign button_link = section.settings.link | default: '#'
  assign button_style = section.settings.button_style
-%}

{%- style -%}
.homepage_banner_title{
   font-size: 31px;
   margin-left: 10px;
   font-weight: 600;
   letter-spacing: 2px;
}
.about-us-divider {
    margin-top: -123px;
}
.about-us-divider-right {
    margin-top: -60px;
}
@media screen and (max-width: 767px) {
.about-us-divider {
    margin-top: -13px;
}
.about-us-divider-right {
    margin-top: -33px;
}
}

.left-slide-image-bg.slide-background.section-image-circle.lazyloaded{
    display: inline-block;
    max-width: 450px;
    text-align: center;
    vertical-align: middle;
    width: 100%;
    line-height: 36px;
    margin-left: 20%;
}  
 
  
.right-slide-image-bg.slide-background.section-image-circle.lazyloaded{
    display: inline-block;
    max-width: 450px;
    text-align: center;
    vertical-align: middle;
    width: 100%;
    line-height: 36px;
    margin-left: 6%;
}

@media screen and (max-width: 1134px) {

  .gallery-content-inner {
     display: inline-block;
     max-width: 310px;
     text-align: center;
     vertical-align: middle;
     width: 100%;
     line-height: 36px;
     margin-left: 0px;
  }

  .gallery-content-inner1 {
      display: inline-block;
      max-width: 310px;
      text-align: left;
      vertical-align: middle;
      width: 100%;
      line-height: 36px;
      margin-left: 2px;
      margin-top: 0px;
  }
  .right-slide-image-bg.slide-background.section-image-circle.lazyloaded {
      display: inline-block;
      max-width: 310px;
      max-height: 310px;
      text-align: center;
      vertical-align: middle;
      width: 100%;
      line-height: 36px;
      margin-left: 26%;
  }
  .left-slide-image-bg.slide-background.section-image-circle.lazyloaded{
    display: inline-block;
    max-width: 310px;
    max-height: 310px;
    text-align: center;
    vertical-align: middle;
    width: 100%;
    line-height: 36px;
  }  
 
}


  
@media screen and (max-width: 767px) {
   .gallery-slider.block.s1.med_s1.line {
    margin-top: 15px;
  }
  .right-slide-image-bg.slide-background.section-image-circle.lazyloaded {
      display: inline-block;
      max-width: 345px;
      max-height: 345px;
      text-align: center;
      vertical-align: middle;
      width: 100%;
      line-height: 36px;
      margin-left: 26%;
  }
  .left-slide-image-bg.slide-background.section-image-circle.lazyloaded{
    display: inline-block;
    max-width: 345px;
    max-height: 345px;
    text-align: center;
    vertical-align: middle;
    width: 100%;
    line-height: 36px;
    margin-left: 20%;
  }  
}

@media screen and (max-width: 568px) {

  .gallery-slider.block.s1.med_s1.line {
    margin-top: 15px;
  }
  
  .right-slide-image-bg.slide-background.section-image-circle.lazyloaded {
      display: inline-block;
      max-width: 271px;
      max-height: 271px;
      text-align: center;
      vertical-align: middle;
      width: 100%;
      line-height: 36px;
      margin-left: 10%;
  }
  .left-slide-image-bg.slide-background.section-image-circle.lazyloaded{
    display: inline-block;
    max-width: 271px;
    max-height: 271px;
    text-align: center;
    vertical-align: middle;
    width: 100%;
    line-height: 36px;
    margin-left: 10%;
  }  
}

.left-gallery-content__text p {
    color:	#3C3A39;
    font-size: 21px;
    font-weight: 500;
    line-height: 30px;
    margin-top: -16%;
}
  

@media screen and (max-width: 767px) {
   .left-gallery-content__text p {
    margin-bottom: 10px;
    font-size: 14px;
    font-weight: 500;
    line-height: 20px;
    margin-top: -46px;
  }
}  
  
.right-gallery-content__text p {
    color: #3c3a39;
    font-size: 21px;
    font-weight: 500;
    line-height: 30px;
    margin-top: 4%;
    margin-left: -14%;
}
  

@media screen and (max-width: 767px) {
    .right-gallery-content__text p {
    margin-bottom: 10px;
    font-size: 14px;
    font-weight: 500;
    line-height: 20px;
    margin-left: 4px;
    margin-top: -67px;
  }
}

  

.gallery-wrap a {
    transition: opacity .2s ease;
    color: #D29896;
}

@media screen and (min-width: 768px) {
  
  .gallery {
    max-width: 1532px;
    margin: 60px auto;
  }

}

@media screen and (max-width: 767px) {


  .gallery {
      margin: 0px 20px 0;
  }
}



a.button.button--primary {
    border-radius: 2em;
	border-color:white;
    color: black;
    background: white;
    font-size: 14px;
    font-weight: 500;
    height: 40px;
}


.gallery-content-inner1 {
    display: inline-block;
    text-align: left;
    vertical-align: middle;
    width: 89%;
    line-height: 36px;
    margin-left: 0px;
    margin-top: 190px;
}

    
.gallery-content-inner {
    display: inline-block;
    max-width: 472px;
    text-align: center;
    vertical-align: middle;
    width: 100%;
    line-height: 36px;
    margin-left: -19px;
}


@media screen and (max-width: 767px) {
  .gallery-content-inner {
     display: inline-block;
     max-width: 270px;
     text-align: center;
     vertical-align: middle;
     width: 100%;
     line-height: 36px;
     margin-left: 0px;
  }

  .gallery-content-inner1 {
      display: inline-block;
      max-width: 270px;
      text-align: left;
      vertical-align: middle;
      width: 100%;
      line-height: 36px;
      margin-left: 2px;
      margin-top: 0px;
  }

}

#gallery-module--{{ section.id }} .gallery-slider {
  --text: {{ section.settings.color_style }};
}

.gallery-slider.block.s1.med_s35 {
  padding: 0;
  height: 100%;
}

@media screen and (max-width: 767px) {
  .gallery-slider.block.s1.med_s35 {
    padding: 0;
    height: 100%;
    display:none;
  }
}

@media screen and (max-width: 767px) {
  .gallery-slider2 {
    height: 100%;
    min-height: 100%;
  }
}  

  .gallery-slider.block.s1.line.med_s1{
    height: 100%;
  }
{%- endstyle -%}

{%- comment -%} Section {%- endcomment -%}

{%- if homepageStyle == 'location' -%}
<section class="gallery-wrap container-wrap" id="gallery-module--{{ section.id }}"
  data-section-id="{{ section.id }}"
  data-section-type="gallery"
  data-dots-style="{{ dots_style }}">
  <div class="gallery">
    <div class="row {{ section.settings.slide_height }}">
      {%- capture slider -%}
      <div class="gallery-slider1 block s1 {{ image_width }}{% if transition == 'zoom-out' %} gallery-slider--zoom-out gallery-slider--is-loading{% endif %}" data-gallery-slider>
        <div class="gallery-slider__inner" data-slider
        data-options="{ 'prevNextButtons': false, 'pageDots':{{ dots }}, 'autoPlay':{{ duration }}, 'adaptiveHeight':{{ image_height }}, 'pauseAutoPlayOnHover':{{ hover }}, 'fade':{{ fade_mode }}, 'setGallerySize':{{ image_height }}, 'wrapAround': true }">
        {%- for block in section.blocks -%}
          {%- assign image = block.settings.slide -%}
          {%- assign image_aspect_ratio = image.aspect_ratio | default: 1.66 -%}

          <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
            data-slide="{{ block.id }}"
            data-slide-index="{{ forloop.index0 }}"
            {{ block.shopify_attributes }}>

            {%- if image -%}
              <div
                class="slide-image-bg slide-background lazyload"
                data-bgset="{% render 'bgset', image: image %}"
                data-sizes="auto"
                data-parent-fit="cover"
                data-block="slide"
                loading="lazy"
                {% if image_height %}
                  style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                {% endif %}
                {{ block.shopify_attributes }}
              >
              </div>
              <noscript>
                <div class="slide-image-bg slide-background no-js-slide"
                style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                ></div>
              </noscript>
            {%- else -%}
              <div class="slide-background__inner">
                <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                  {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                  {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                </div>
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
        </div>

        {%- if show_nav_arrows and section.blocks.size > 1 -%}
          <button class="slider-arrow slider-arrow--prev" aria-label="{{ 'slideshow.previous' | t }}" type="button" data-prev-arrow>{% render "icon-submit" %}</button>
          <button class="slider-arrow slider-arrow--next" aria-label="{{ 'slideshow.next' | t }}" type="button" data-next-arrow>{% render "icon-submit" %}</button>
        {%- endif -%}
      </div>
      
      {%- endcapture -%}
      {{ slider }}
      


      {%- if title != blank or subheading != blank or text != blank or button_text != blank -%}
      <div class="gallery-content1 block s1 {{ content_width }}">
        
          {%- assign image = section.settings.slide_main -%}
          {%- if image -%}
          <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
            data-slide="{{ block.id }}"
            data-slide-index="{{ forloop.index0 }}"
            {{ block.shopify_attributes }}>

            {%- if image -%}
              <div
                class="slide-image-bg slide-background lazyload"
                data-bgset="{% render 'bgset', image: image %}"
                data-sizes="auto"
                data-parent-fit="cover"
                data-block="slide"
                loading="lazy"
                {% if image_height %}
                  style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                {% endif %}
                {{ block.shopify_attributes }}
              >
              </div>
              <noscript>
                <div class="slide-image-bg slide-background no-js-slide"
                style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                ></div>
              </noscript>
            {%- else -%}
              <div class="slide-background__inner">
                <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                  {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                  {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                </div>
              </div>
            {%- endif -%}
          </div>
         {% endif %}
        
        
        <div class="gallery-content-inner1">
          {%- if subheading != blank -%}
            <p class="subheading-text"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
              {{- subheading -}}
            </p>
            {%- assign animation_delay = animation_delay | plus: 250 -%}
          {%- endif -%}

          {%- if title != blank -%}
            <h2 id="shopify-section-template--{{ section.id }}__image-with-text" {% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
              {{- title -}}
            </h2>
            {%- assign animation_delay = animation_delay | plus: 250 -%}
          {%- endif -%}

          {%- if text != blank -%}
            <div id="shopify-section-template--{{ section.id }}__image-with-text" class="gallery-content__text"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
              {{- text -}}
            </div>
            {%- assign animation_delay = animation_delay | plus: 250 -%}
          {%- endif -%}

          {%- if button_text != blank -%}
            <div class="gallery-content__button"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
              <a {% if button_link != blank %}href="{{ button_link }}"{% endif %} class="button button--{{ button_style }}">{{ button_text }}</a>
            </div>
          {%- endif -%}
        </div>
      </div>
      {%- endif -%}
  
      {%- capture slider -%}
      <div class="gallery-slider2 block s1 {{ image_width }}{% if transition == 'zoom-out' %} gallery-slider--zoom-out gallery-slider--is-loading{% endif %}" data-gallery-slider>
        <div class="gallery-slider__inner" data-slider
        data-options="{ 'prevNextButtons': false, 'pageDots':{{ dots }}, 'autoPlay':{{ duration }}, 'adaptiveHeight':{{ image_height }}, 'pauseAutoPlayOnHover':{{ hover }}, 'fade':{{ fade_mode }}, 'setGallerySize':{{ image_height }}, 'wrapAround': true }">
        {%- for block in section.blocks -%}
          {%- assign image = block.settings.slide -%}
          {%- assign image_aspect_ratio = image.aspect_ratio | default: 1.66 -%}

          <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
            data-slide="{{ block.id }}"
            data-slide-index="{{ forloop.index0 }}"
            {{ block.shopify_attributes }}>

            {%- if image -%}
              <div
                class="slide-image-bg slide-background lazyload"
                data-bgset="{% render 'bgset', image: image %}"
                data-sizes="auto"
                data-parent-fit="cover"
                data-block="slide"
                loading="lazy"
                {% if image_height %}
                  style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                {% endif %}
                {{ block.shopify_attributes }}
              >
              </div>
              <noscript>
                <div class="slide-image-bg slide-background no-js-slide"
                style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                ></div>
              </noscript>
            {%- else -%}
              <div class="slide-background__inner">
                <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                  {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                  {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                </div>
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
        </div>

        {%- if show_nav_arrows and section.blocks.size > 1 -%}
          <button class="slider-arrow slider-arrow--prev" aria-label="{{ 'slideshow.previous' | t }}" type="button" data-prev-arrow>{% render "icon-submit" %}</button>
          <button class="slider-arrow slider-arrow--next" aria-label="{{ 'slideshow.next' | t }}" type="button" data-next-arrow>{% render "icon-submit" %}</button>
        {%- endif -%}
      </div>
      {%- endcapture -%}
       {{ slider }}
    </div>
  </div>
</section>

{%- elsif  homepageStyle == 'aboutus' -%}

    {%- if image_position == 'left' -%}

     <section class="about-us-divider gallery-wrap container-wrap" id="gallery-module--{{ section.id }}"
        data-section-id="{{ section.id }}"
        data-section-type="gallery"
        data-dots-style="{{ dots_style }}">
        <div class="gallery">
          <div class="row {{ section.settings.slide_height }}">
            {%- capture slider -%}
            <div class="gallery-slider2 block s1 {{ image_width }}{% if transition == 'zoom-out' %} gallery-slider--zoom-out gallery-slider--is-loading{% endif %}" data-gallery-slider>
              <div class="gallery-slider__inner" data-slider
              data-options="{ 'prevNextButtons': false, 'pageDots':{{ dots }}, 'autoPlay':{{ duration }}, 'adaptiveHeight':{{ image_height }}, 'pauseAutoPlayOnHover':{{ hover }}, 'fade':{{ fade_mode }}, 'setGallerySize':{{ image_height }}, 'wrapAround': true }">
              {%- for block in section.blocks -%}
                {%- assign image = block.settings.slide -%}
                {%- assign image_aspect_ratio = image.aspect_ratio | default: 1.66 -%}

                <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
                  data-slide="{{ block.id }}"
                  data-slide-index="{{ forloop.index0 }}"
                  {{ block.shopify_attributes }}>

                  {%- if image -%}
                  {%- if imageStyle == 'round' -%}
                    <div
                      class="left-slide-image-bg slide-background lazyload section-image-circle"
                      data-bgset="{% render 'bgset', image: image %}"
                      data-sizes="auto"
                      data-parent-fit="cover"
                      data-block="slide"
                      loading="lazy"
                      {% if image_height %}
                        style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                      {% endif %}
                      {{ block.shopify_attributes }}
                    >
                    </div>

                  {%- else -%}
                    <div
                      class="slide-image-bg slide-background lazyload "
                      data-bgset="{% render 'bgset', image: image %}"
                      data-sizes="auto"
                      data-parent-fit="cover"
                      data-block="slide"
                      loading="lazy"
                      {% if image_height %}
                        style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                      {% endif %}
                      {{ block.shopify_attributes }}
                    >
                    </div>

                  {%- endif -%}
                    <noscript>
                      <div class="slide-image-bg slide-background no-js-slide"
                      style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                      ></div>
                    </noscript>
                  {%- else -%}
                    <div class="slide-background__inner">
                      <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                        {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                        {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                      </div>
                    </div>
                  {%- endif -%}
                </div>
              {%- endfor -%}
              </div>

              {%- if show_nav_arrows and section.blocks.size > 1 -%}
                <button class="slider-arrow slider-arrow--prev" aria-label="{{ 'slideshow.previous' | t }}" type="button" data-prev-arrow>{% render "icon-submit" %}</button>
                <button class="slider-arrow slider-arrow--next" aria-label="{{ 'slideshow.next' | t }}" type="button" data-next-arrow>{% render "icon-submit" %}</button>
              {%- endif -%}
            </div>
            {%- endcapture -%}

            {{ slider }}

            {%- if title != blank or subheading != blank or text != blank or button_text != blank -%}
            <div class="gallery-content-aboutus  block s1 {{ content_width }}">

                {%- assign image = section.settings.slide_main -%}
                {%- if image -%}
                <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
                  data-slide="{{ block.id }}"
                  data-slide-index="{{ forloop.index0 }}"
                  {{ block.shopify_attributes }}>

                  {%- if image -%}
                    {%- if imageStyle == 'round' -%}
                    <div
                      class="slide-image-bg slide-background lazyload section-image-circle"
                      data-bgset="{% render 'bgset', image: image %}"
                      data-sizes="auto"
                      data-parent-fit="cover"
                      data-block="slide"
                      loading="lazy"
                      {% if image_height %}
                        style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                      {% endif %}
                      {{ block.shopify_attributes }}
                    >
                    </div>

                  {%- else -%}
                    <div
                      class="slide-image-bg slide-background lazyload "
                      data-bgset="{% render 'bgset', image: image %}"
                      data-sizes="auto"
                      data-parent-fit="cover"
                      data-block="slide"
                      loading="lazy"
                      {% if image_height %}
                        style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                      {% endif %}
                      {{ block.shopify_attributes }}
                    >
                    </div>

                  {%- endif -%}

                    <noscript>
                      <div class="slide-image-bg slide-background no-js-slide"
                      style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                      ></div>
                    </noscript>
                  {%- else -%}
                    <div class="slide-background__inner">
                      <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                        {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                        {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                      </div>
                    </div>
                  {%- endif -%}
                </div>
               {% endif %}


              <div class="gallery-content-inner">
                {%- if subheading != blank -%}
                  <p class="subheading-text"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
                    {{- subheading -}}
                  </p>
                  {%- assign animation_delay = animation_delay | plus: 250 -%}
                {%- endif -%}

                {%- if title != blank -%}
                  <h2 id="shopify-section-template--{{ section.id }}__image-with-text" {% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %} >
                    {{- title -}}
                  </h2>
                  {%- assign animation_delay = animation_delay | plus: 250 -%}
                {%- endif -%}

                {%- if text != blank -%}
                  <div id="shopify-section-template--{{ section.id }}__image-with-text" class="right-gallery-content__text"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
                    {{- text -}}
                  </div>
                  {%- assign animation_delay = animation_delay | plus: 250 -%}
                {%- endif -%}

                {%- if button_text != blank -%}
                  <div class="gallery-content__button"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
                    <a {% if button_link != blank %}href="{{ button_link }}"{% endif %} class="button button--{{ button_style }}">{{ button_text }}</a>
                  </div>
                {%- endif -%}
              </div>
            </div>
            
            
            {%- capture slider -%}
            <div class="gallery-slider1 block s1 {{ image_width }}{% if transition == 'zoom-out' %} gallery-slider--zoom-out gallery-slider--is-loading{% endif %}" data-gallery-slider>
              <div class="gallery-slider__inner" data-slider
              data-options="{ 'prevNextButtons': false, 'pageDots':{{ dots }}, 'autoPlay':{{ duration }}, 'adaptiveHeight':{{ image_height }}, 'pauseAutoPlayOnHover':{{ hover }}, 'fade':{{ fade_mode }}, 'setGallerySize':{{ image_height }}, 'wrapAround': true }">
              {%- for block in section.blocks -%}
                {%- assign image = block.settings.slide -%}
                {%- assign image_aspect_ratio = image.aspect_ratio | default: 1.66 -%}

                <div class="right-slide-image{% unless image %} slide-image--placeholder{% endunless %}"
                  data-slide="{{ block.id }}"
                  data-slide-index="{{ forloop.index0 }}"
                  {{ block.shopify_attributes }}>

                  {%- if image -%}
                    {%- if imageStyle == 'round' -%}
                      <div
                        class="right-slide-image-bg slide-background lazyload section-image-circle"
                        data-bgset="{% render 'bgset', image: image %}"
                        data-sizes="auto"
                        data-parent-fit="cover"
                        data-block="slide"
                        loading="lazy"
                        {% if image_height %}
                          style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                        {% endif %}
                        {{ block.shopify_attributes }}
                      >
                      </div>

                    {%- else -%}
                      <div
                        class="slide-image-bg slide-background lazyload "
                        data-bgset="{% render 'bgset', image: image %}"
                        data-sizes="auto"
                        data-parent-fit="cover"
                        data-block="slide"
                        loading="lazy"
                        {% if image_height %}
                          style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                        {% endif %}
                        {{ block.shopify_attributes }}
                      >
                      </div>

                    {%- endif -%}

                    <noscript>
                      <div class="slide-image-bg slide-background no-js-slide"
                      style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                      ></div>
                    </noscript>
                  {%- else -%}
                    <div class="slide-background__inner">
                      <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                        {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                        {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                      </div>
                    </div>
                  {%- endif -%}
                </div>
              {%- endfor -%}
              </div>

              {%- if show_nav_arrows and section.blocks.size > 1 -%}
                <button class="slider-arrow slider-arrow--prev" aria-label="{{ 'slideshow.previous' | t }}" type="button" data-prev-arrow>{% render "icon-submit" %}</button>
                <button class="slider-arrow slider-arrow--next" aria-label="{{ 'slideshow.next' | t }}" type="button" data-next-arrow>{% render "icon-submit" %}</button>
              {%- endif -%}
            </div>
            {%- endcapture -%}
             {{ slider }}
            {%- endif -%}

            {%- if image_position == 'right' -%}
              {{- slider -}}
            {%- endif -%}
          </div>
        </div>
      </section>


    {%- else -%}
    <section class="about-us-divider-right gallery-wrap container-wrapgallery-wrap container-wrap" id="gallery-module--{{ section.id }}"
      data-section-id="{{ section.id }}"
      data-section-type="gallery"
      data-dots-style="{{ dots_style }}">
      <div class="gallery">
        <div class="row {{ section.settings.slide_height }}">
          {%- capture slider -%}
          <div class="gallery-slider block s1 {{ image_width }}{% if transition == 'zoom-out' %} gallery-slider--zoom-out gallery-slider--is-loading{% endif %}" data-gallery-slider>
            <div class="gallery-slider__inner" data-slider
            data-options="{ 'prevNextButtons': false, 'pageDots':{{ dots }}, 'autoPlay':{{ duration }}, 'adaptiveHeight':{{ image_height }}, 'pauseAutoPlayOnHover':{{ hover }}, 'fade':{{ fade_mode }}, 'setGallerySize':{{ image_height }}, 'wrapAround': true }">
            {%- for block in section.blocks -%}
              {%- assign image = block.settings.slide -%}
              {%- assign image_aspect_ratio = image.aspect_ratio | default: 1.66 -%}

              <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
                data-slide="{{ block.id }}"
                data-slide-index="{{ forloop.index0 }}"
                {{ block.shopify_attributes }}>

                {%- if image -%}
                {%- if imageStyle == 'round' -%}
                  <div
                    class="right-slide-image-bg slide-background lazyload section-image-circle"
                    data-bgset="{% render 'bgset', image: image %}"
                    data-sizes="auto"
                    data-parent-fit="cover"
                    data-block="slide"
                    loading="lazy"
                    {% if image_height %}
                      style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                    {% endif %}
                    {{ block.shopify_attributes }}
                  >
                  </div>

                {%- else -%}
                  <div
                    class="slide-image-bg slide-background lazyload "
                    data-bgset="{% render 'bgset', image: image %}"
                    data-sizes="auto"
                    data-parent-fit="cover"
                    data-block="slide"
                    loading="lazy"
                    {% if image_height %}
                      style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                    {% endif %}
                    {{ block.shopify_attributes }}
                  >
                  </div>

                {%- endif -%}
                  <noscript>
                    <div class="slide-image-bg slide-background no-js-slide"
                    style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                    ></div>
                  </noscript>
                {%- else -%}
                  <div class="slide-background__inner">
                    <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                      {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                      {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                    </div>
                  </div>
                {%- endif -%}
              </div>
            {%- endfor -%}
            </div>

            {%- if show_nav_arrows and section.blocks.size > 1 -%}
              <button class="slider-arrow slider-arrow--prev" aria-label="{{ 'slideshow.previous' | t }}" type="button" data-prev-arrow>{% render "icon-submit" %}</button>
              <button class="slider-arrow slider-arrow--next" aria-label="{{ 'slideshow.next' | t }}" type="button" data-next-arrow>{% render "icon-submit" %}</button>
            {%- endif -%}
          </div>
          {%- endcapture -%}

          {%- if image_position == 'left' -%}
            {{ slider }}
          {%- endif -%}

          {%- if title != blank or subheading != blank or text != blank or button_text != blank -%}
          <div class="gallery-content-aboutus block s1 {{ content_width }}">

              {%- assign image = section.settings.slide_main -%}
              {%- if image -%}
              <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
                data-slide="{{ block.id }}"
                data-slide-index="{{ forloop.index0 }}"
                {{ block.shopify_attributes }}>

                {%- if image -%}
                  {%- if imageStyle == 'round' -%}
                  <div
                    class="left-slide-image-bg slide-background lazyload section-image-circle"
                    data-bgset="{% render 'bgset', image: image %}"
                    data-sizes="auto"
                    data-parent-fit="cover"
                    data-block="slide"
                    loading="lazy"
                    {% if image_height %}
                      style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                    {% endif %}
                    {{ block.shopify_attributes }}
                  >
                  </div>

                {%- else -%}
                  <div
                    class="left-slide-image-bg slide-background lazyload "
                    data-bgset="{% render 'bgset', image: image %}"
                    data-sizes="auto"
                    data-parent-fit="cover"
                    data-block="slide"
                    loading="lazy"
                    {% if image_height %}
                      style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                    {% endif %}
                    {{ block.shopify_attributes }}
                  >
                  </div>

                {%- endif -%}

                  <noscript>
                    <div class="left-slide-image-bg slide-background no-js-slide"
                    style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                    ></div>
                  </noscript>
                {%- else -%}
                  <div class="slide-background__inner">
                    <div class="left-slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                      {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                      {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                    </div>
                  </div>
                {%- endif -%}
              </div>
             {% endif %}


            <div class="gallery-content-inner">
              {%- if subheading != blank -%}
                <p class="subheading-text"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
                  {{- subheading -}}
                </p>
                {%- assign animation_delay = animation_delay | plus: 250 -%}
              {%- endif -%}

              {%- if title != blank -%}
                <h2 id="shopify-section-template--{{ section.id }}__image-with-text" {% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
                  {{- title -}}
                </h2>
                {%- assign animation_delay = animation_delay | plus: 250 -%}
              {%- endif -%}

              {%- if text != blank -%}
                <div id="shopify-section-template--{{ section.id }}__image-with-text" class="left-gallery-content__text"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
                  {{- text -}}
                </div>
                {%- assign animation_delay = animation_delay | plus: 250 -%}
              {%- endif -%}

              {%- if button_text != blank -%}
                <div class="gallery-content__button"{% if transition_text %} data-aos="fade-up" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
                  <a {% if button_link != blank %}href="{{ button_link }}"{% endif %} class="button button--{{ button_style }}">{{ button_text }}</a>
                </div>
              {%- endif -%}
            </div>
          </div>
          {%- endif -%}

          {%- if image_position == 'right' -%}
            {{- slider -}}
          {%- endif -%}
        </div>
      </div>
    </section>
     {%- endif -%}



{%- else -%}

<section class="gallery-wrap container-wrap" id="gallery-module--{{ section.id }}"
  data-section-id="{{ section.id }}"
  data-section-type="gallery"
  data-dots-style="{{ dots_style }}">
  <div class="gallery">
    <div class="row {{ section.settings.slide_height }}">
      {%- capture slider -%}
        
         {%- if image_width == 'med_s1' -%}
            <div class="gallery-slider block s1 line {{ image_width }}{% if transition == 'zoom-out' %} gallery-slider--zoom-out gallery-slider--is-loading{% endif %}" style="height:100%;min-height: 100%;" data-gallery-slider>         
         {%- else -%}
            <div class="gallery-slider block s1 {{ image_width }}{% if transition == 'zoom-out' %} gallery-slider--zoom-out gallery-slider--is-loading{% endif %}" data-gallery-slider>       
         {%- endif -%}
        
        <div class="gallery-slider__inner" data-slider
        data-options="{ 'prevNextButtons': false, 'pageDots':{{ dots }}, 'autoPlay':{{ duration }}, 'adaptiveHeight':{{ image_height }}, 'pauseAutoPlayOnHover':{{ hover }}, 'fade':{{ fade_mode }}, 'setGallerySize':{{ image_height }}, 'wrapAround': true }">
        {%- for block in section.blocks -%}
          
          {%- assign image = block.settings.slide -%}
          {%- assign image_aspect_ratio = image.aspect_ratio | default: 1.66 -%}

          <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
            data-slide="{{ block.id }}"
            data-slide-index="{{ forloop.index0 }}"
            {{ block.shopify_attributes }}>

            {%- if image -%}
              <div
                class="slide-image-bg slide-background lazyload"
                data-bgset="{% render 'bgset', image: image %}"
                data-sizes="auto"
                data-parent-fit="cover"
                data-block="slide"
                loading="lazy"
                {% if image_height %}
                  style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                {% endif %}
                {{ block.shopify_attributes }}
              >
              </div>
              <noscript>
                <div class="slide-image-bg slide-background no-js-slide"
                style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                ></div>
              </noscript>
            {%- else -%}
              <div class="slide-background__inner">
                <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                  {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                  {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                </div>
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
        </div>

        {%- if show_nav_arrows and section.blocks.size > 1 -%}
          <button class="slider-arrow slider-arrow--prev" aria-label="{{ 'slideshow.previous' | t }}" type="button" data-prev-arrow>{% render "icon-submit" %}</button>
          <button class="slider-arrow slider-arrow--next" aria-label="{{ 'slideshow.next' | t }}" type="button" data-next-arrow>{% render "icon-submit" %}</button>
        {%- endif -%}
      </div>
      {%- endcapture -%}

      {%- if image_position == 'left' -%}
        {{ slider }}
      {%- endif -%}

      {%- if title != blank or subheading != blank or text != blank or button_text != blank -%}
      <div class="gallery-content block s1 {{ content_width }}">
        
          {%- assign image = section.settings.slide_main -%}
          {%- if image -%}
          
  
          <div class="slide-image{% unless image %} slide-image--placeholder{% endunless %}"
            data-slide="{{ block.id }}"
            data-slide-index="{{ forloop.index0 }}"
            {{ block.shopify_attributes }}>

            {%- if image -%}
              <div
                class="slide-image-bg slide-background lazyload"
                data-bgset="{% render 'bgset', image: image %}"
                data-sizes="auto"
                data-parent-fit="cover"
                data-block="slide"
                loading="lazy"
                {% if image_height %}
                  style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }}"
                {% endif %}
                {{ block.shopify_attributes }}
              >
              </div>
              <noscript>
                <div class="slide-image-bg slide-background no-js-slide"
                style="background-image:url('{{ block.settings.slide | img_url: '1024x' }}');{% if image_height %}padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};{% endif %}"
                ></div>
              </noscript>
            {%- else -%}
              <div class="slide-background__inner">
                <div class="slide-image-bg slide-background--blank"{% if image_height %} style="padding-top: {{ 100 | divided_by: image_aspect_ratio | append: '%' }};"{% endif %}>
                  {%- capture current -%}{% cycle 1, 2 %}{%- endcapture -%}
                  {{- 'lifestyle-' | append: current | placeholder_svg_tag: 'placeholder-svg' -}}
                </div>
              </div>
            {%- endif -%}
          </div>
         {% endif %}
        
        
        <div class="gallery-content-inner">
          {%- if subheading != blank -%}
            <p class="subheading-text"{% if transition_text %} data-aos="fade-up1" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
              {{- subheading -}}
            </p>
            {%- assign animation_delay = animation_delay | plus: 250 -%}
          {%- endif -%}

          {%- if title != blank -%}
            <h2 class="homepage_banner_title" style="color:{{ title_color }}" id="shopify-section-template--{{ section.id }}__image-with-text" {% if transition_text %} data-aos="fade-up1" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
              {{- title -}}
            </h2>
            {%- assign animation_delay = animation_delay | plus: 250 -%}
          {%- endif -%}

          {%- if text != blank -%}
            <div id="shopify-section-template--{{ section.id }}__image-with-text" class="gallery-content__text"{% if transition_text %} data-aos="fade-up1" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
              {{- text -}}
            </div>
            {%- assign animation_delay = animation_delay | plus: 250 -%}
          {%- endif -%}

          {%- if button_text != blank -%}
            <div class="gallery-content__button"{% if transition_text %} data-aos="fade-up1" data-aos-delay="{{ animation_delay }}" data-aos-anchor="{{ animation_anchor }}"{% endif %}>
              <a {% if button_link != blank %}href="{{ button_link }}"{% endif %} class="button button--{{ button_style }}">{{ button_text }}</a>
            </div>
          {%- endif -%}
        </div>
      </div>
      {%- endif -%}

      {%- if image_position == 'right' -%}
        {{- slider -}}
      {%- endif -%}
    </div>
  </div>
</section>
{%- endif -%}

{% comment %} Schema {% endcomment %}
{% schema %}
{
  "name": "Image with text",
  "class": "shopify-section--image-with-text2",
  "max_blocks": 6,
  "settings": [
    {
      "type": "header",
      "content": "Slideshow"
    },
    {
      "type": "image_picker",
      "id": "slide_main",
      "label": "Image"
    },
    {
      "type": "checkbox",
      "id": "show_nav_arrows",
      "label": "Show navigation arrows",
      "default": true
    },
    {
      "type": "select",
      "id": "color_style",
      "label": "Arrows and dots color",
      "default": "white",
      "options": [
        {
          "value": "white",
          "label": "Light"
        },
        {
          "value": "black",
          "label": "Dark"
        }
      ]
    },
    {
      "type": "select",
      "id": "transition",
      "label": "Transition style",
      "default": "fade",
      "options": [
        { "label": "Slide", "value": "slide" },
        { "label": "Fade", "value": "fade" },
        { "label": "Zoom out", "value": "zoom-out" }
      ]
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Autoplay slideshow",
      "default": true
    },
    {
      "type": "range",
      "id": "duration",
      "min": 2,
      "max": 5,
      "step": 1,
      "unit": "sec",
      "label": "Change slides every",
      "default": 4
    },
    {
      "type": "select",
      "id": "slide_height",
      "label": "Slide height",
      "default": "five-fifty-height-hero",
      "options": [
        {
          "value": "one-hundred-height-hero",
          "label": "Full screen height"
        },
        {
          "value": "original-height",
          "label": "Image height"
        },
        {
          "value": "seven-fifty-height-hero",
          "label": "750px"
        },
        {
          "value": "sixty-fifty-height-hero",
          "label": "650px"
        },
        {
          "value": "five-fifty-height-hero",
          "label": "550px"
        },
        {
          "value": "four-fifty-height-hero",
          "label": "450px"
        }
      ]
    },
    {
      "type": "select",
      "id": "slide_image_position",
      "label": "Image position",
      "options": [
          {
            "value": "left",
            "label": "Left"
          },
          {
            "value": "right",
            "label": "Right"
          }
        ],
        "default": "left"
    },
    {
      "type": "select",
      "id": "slide_image_width",
      "label": "Image width",
      "options": [
        {
          "value": "med_s12|med_s12",
          "label": "50%"
        },
        {
          "value": "med_s35|med_s25",
          "label": "60%"
        },
        {
          "value": "med_s34|med_s14",
          "label": "75%"
        },
        {
          "value": "med_s1",
          "label": "100%"
        }
      ],
      "default": "med_s12|med_s12"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Humble Beginnings"
    },
    {
      "type": "textarea",
      "id": "title",
      "label": "Heading",
      "default": "Tell a Story"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Text Color",
      "default": "#ffffff"
    },    
    {
      "type": "richtext",
      "id": "content",
      "label": "Text",
      "default": "<p>By showcasing a product, collection, or your brand.</p>"
    },
    {
      "type": "header",
      "content": "Button"
    },
    {
      "type": "text",
      "id": "cta",
      "label": "Label"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Link"
    },
    {
      "type": "select",
      "id": "button_style",
      "label": "Style",
      "default": "primary",
      "options": [
        { "label": "Primary", "value": "primary" },
        { "label": "Secondary", "value": "secondary" }
      ]
    },
    {
      "type": "select",
      "id": "image_style",
      "label": "Style",
      "default": "round",
      "options": [
        { "label": "Round", "value": "round" },
        { "label": "Rectangle", "value": "rectangle" }
      ]
    },
    {
      "type": "select",
      "id": "homepage_style",
      "label": "Homepage Style",
      "default": "none",
      "options": [
        { "label": "None", "value": "none" },
        { "label": "AboutUs", "value": "aboutus" },
        { "label": "Location", "value": "location" }
      ]
    }
  ],
  "blocks": [
    {
      "type": "gallery_image",
      "name": "Image",
      "settings": [
        {
          "type": "image_picker",
          "id": "slide",
          "label": "Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "category": "Image",
      "name": "Image with text",
      "blocks": [
        {
          "type": "gallery_image"
        },
        {
          "type": "gallery_image"
        }
      ]
    }
  ]
}
{% endschema %}